//NUMBER GAME

import java.util.Random;
import java.util.Scanner;

public class NumberGuessingGame {

    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);
        Random random = new Random();
        int minRange = 1;
        int maxRange = 100;
        int maxAttempts = 7; // You can adjust the number of attempts
        int roundsWon = 0;

        boolean playAgain = true;
        while (playAgain) {
            int secretNumber = random.nextInt(maxRange - minRange + 1) + minRange;
            int attempts = 0;

            System.out.println("Welcome to the Number Guessing Game!");
            System.out.printf("Guess the number between %d and %d.\n", minRange, maxRange);

            while (attempts < maxAttempts) {
                System.out.print("Enter your guess: ");
                if (!scanner.hasNextInt()) {
                    System.out.println("Invalid input. Please enter a number.");
                    scanner.next(); // Consume invalid input
                    continue; // Skip to the next iteration of the loop
                }
                int guess = scanner.nextInt();
                attempts++;

                if (guess < minRange || guess > maxRange) {
                    System.out.println("Please enter a number within the specified range.");
                    continue; // Skip to the next iteration of the loop
                }

                if (guess == secretNumber) {
                    System.out.printf("Congratulations! You guessed the number in %d attempts.\n", attempts);
                    roundsWon++;
                    break; // Exit the inner loop since the user guessed correctly
                } else if (guess < secretNumber) {
                    System.out.println("Too low!");
                } else {
                    System.out.println("Too high!");
                }

                if (attempts == maxAttempts) {
                    System.out.printf("You ran out of attempts. The number was %d.\n", secretNumber);
                }
            }

            System.out.print("Do you want to play again? (yes/no): ");
            String playChoice = scanner.next().toLowerCase();
            playAgain = playChoice.equals("yes");
        }

        System.out.printf("Thanks for playing! You won %d round(s).\n", roundsWon);
        scanner.close();
    }
}

OUTPUT :

Welcome to the Number Guessing Game!
Guess the number between 1 and 100.
Enter your guess: 50
Too high!
Enter your guess: 25
Too low!
Enter your guess: 37
Too low!
Enter your guess: 43
Too high!
Enter your guess: 40
Congratulations! You guessed the number in 5 attempts.
Do you want to play again? (yes/no): yes
Welcome to the Number Guessing Game!
Guess the number between 1 and 100.
Enter your guess: 60
Too low!
Enter your guess: 80
Too high!
Enter your guess: 70
Too low!
Enter your guess: 75
Too high!
Enter your guess: 73
You ran out of attempts. The number was 72.
Do you want to play again? (yes/no): no
Thanks for playing! You won 1 round(s).
